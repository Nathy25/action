name: Hola Mundo

on: 
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    hola:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              run: |
                echo "Hola mundo"
                echo "Nathy is 33 years old and lives in Rosario."
               
            - name: Touch file
              run: touch hola.txt

            - name: Touch file
              run: touch hola.txt

            - name: Set text
              run: echo "Hola mundo" > hola.txt

            - name: Read file
              run: cat hola.txt 




    other: 
        runs-on: ubuntu-latest
        needs: hola
        steps:
            - name: ls
              run: ls -al